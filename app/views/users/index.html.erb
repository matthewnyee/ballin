<h1>Reed College Basketball</h1>
<p> Thank you for RSVPing before coming to play Reed basketball! Please click on your username to edit your status, or click <a href='/users/new'>here</a> to create a new user.</p>

<%= link_to "Create new user", new_user_path %>
<br>
<br>
<table class = 'table table-hover'>
  <tr>
    <td> Username </td>
    <td> Status </td>
    <td>  </td>
  </tr>
  <% if @users %>
    <% @users.each do |user| %>
      <tr>
      <td> <%= link_to "#{user.name}", edit_user_path(user.id) %> </td>
      <td> <%= user.status %> </td>
      <td> <%= link_to "Delete", user_path(user.id), class: 'btn',
                  method: :delete, data: { confirm: "Are you sure you want to delete #{user.name}?" } %> </td>
      </tr>
    <% end %>
    <% end %>
</table>
